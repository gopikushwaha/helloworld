name: CI

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: gopikushwaha/helloworld

    - name: Set up Python
      uses: gopikushwaha/helloworld
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        echo "Hello World"

    - name: Run tests
      run: |
        echo "DONE"
